# Smart Fan Controller - Python függőségek
#
# Python >= 3.11 szükséges
# Telepítés:   pip install -r requirements.txt
# Alternatíva: pip install -e ".[all]"   (a pyproject.toml extras-ai)
#
# Megjegyzés: runtime-ban minden alábbi könyvtár opcionális – a program a
# rendelkezésre álló könyvtárak alapján automatikusan engedélyezi/letiltja az
# adott funkciót (adatforrás, HUD, auto-launch). Ez a fájl a TELJES
# funkcionalitáshoz telepíti mindet. Az npcap NEM szükséges a programhoz.

# --- BLE kommunikáció: ventilátor kimenet + BLE power/HR szenzorok ---
#     (smart_fan_controller.handlers._ble)
bleak>=1.0.0

# --- ANT+ kommunikáció: power meter, HR monitor ---
#     (smart_fan_controller.handlers._ant)
openant>=1.3.0

# --- Zwift HTTPS API polling: a "zwiftudp" adatforrás segédprocessze ---
#     (smart_fan_controller.zwift_api – külön processzben fut, UDP-n továbbít a fő appnak)
requests>=2.32.0

# --- Zwift auto-launch: Windows UI automatizáció (opcionális, csak Windows) ---
pywinauto>=0.6.9

# --- HUD: PySide6 / Qt6 LCARS felület (opcionális – nélküle headless módban fut) ---
#     (smart_fan_controller.ui)
PySide6>=6.7.0

# --- Standard library (nem kell telepíteni, csak referencia) ---
# asyncio, threading, json, signal, subprocess, collections, dataclasses,
# enum, math, logging, time, atexit, os, sys, platform, copy,
# wave, struct, io, tempfile (a core.helpers LCARS hangeffekt-generáláshoz)
