# Smart Fan Controller - Python dependencies
#
# Python >= 3.10 szükséges
# Telepítés: pip install -r requirements.txt
#
# Megjegyzés: az openant és a bleak opcionálisak –
# a program a rendelkezésre álló könyvtárak alapján
# automatikusan engedélyezi/letiltja az adatforrásokat.
# Az npcap NEM szükséges a programhoz.

# --- BLE kommunikáció (ventilátor vezérlés + BLE szenzorok) ---
bleak>=0.21.0

# --- ANT+ kommunikáció (power meter, HR monitor) ---
openant>=1.0.0

# --- Zwift API polling (zwiftudp adatforrás, zwift_api_polling.py) ---
requests>=2.28.0

# --- Zwift auto-launch (Windows UI automatizáció, opcionális) ---
pywinauto>=0.6.8

# --- HUD (PySide6 / Qt6, opcionális – nélküle headless módban fut) ---
PySide6>=6.5.0

# --- Standard library (nem kell telepíteni, referencia) ---
# asyncio, threading, json, signal, subprocess, abc,
# collections, dataclasses, enum, math, logging, time, atexit, os, sys,
# wave, struct, io, tempfile (LCARS hangeffekt generáláshoz)
